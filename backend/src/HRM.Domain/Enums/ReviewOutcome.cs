namespace HRM.Domain.Enums;

/// <summary>HR Staff's screening outcome for one employee within a review period (US-04).</summary>
public enum ReviewOutcome
{
    PENDING,
    APPROVED,
    REJECTED
}
